# Stream 9 — Final Assembly Report

**Phase:** 3
**Date:** 2026-04-29
**Branch:** rewrite-v3.1.0

---

## Summary

Final assembly and verification of the YuriKey module after all Phase 2 streams completed. Built the module, fixed build issues, verified structure, cleaned old directories, and updated git tracking.

---

## Steps Executed

### 1. Pre-Build Fixes

Three issues found before the build could succeed:

| Issue | Fix |
|-------|-----|
| `src/module.prop` missing | Copied from `Module/module.prop` |
| `src/META-INF/` missing | Copied from `Module/META-INF/` |
| `src/webroot/js/bridge.js` missing `exec` export | Added `exec()` as alias for `runScriptRaw()` — `cfg.js` and `history.js` depend on it |

### 2. Build

```sh
npm install         # 130 packages (no lockfile existed; npm ci would fail)
npm run build       # Parcel bundled webroot, build:module assembled Module/
```

**Build output:**
- `Module/webroot/` — Parcel-bundled index.html + hashed CSS/JS bundles
- `Module/lib/` — 5 shell library files
- `Module/features/` — 14 feature scripts
- `Module/pipelines/` — 2 pipeline definitions
- `Module/rka/` — 2 RKA scripts (no sqlite3 binaries)
- `Module/*.sh` — 6 root-level scripts (orchestrator, customize, service, boot-completed, uninstall, action)

### 3. Verification — All Checks Pass

| # | Check | Result |
|---|-------|--------|
| 1 | No hardcoded `/data/adb/modules/Yurikey` paths | OK |
| 2 | No `return` in feature scripts | OK |
| 3 | Features use `../lib/` sourcing | OK |
| 4 | Root scripts use `$MODDIR/lib/` | OK |
| 5 | `customize.sh` uses `$MODPATH` | OK |
| 6 | `action.sh` context detection (exit/return) | OK |
| 7 | No `su -c` in features | OK |
| 8 | `config_env.sh` has `cfg_get`/`cfg_set`/`cfg_delete` | OK |
| 9 | `device-info.sh` sources from `lib/` | OK |
| 10 | Bridge detection present in bundled JS | OK |
| 11 | CFG cache present in bundled JS | OK |
| 12 | `addHistory` uses `printf` (shell-safe) | OK |
| 13 | `i18n` handles `en → source/string.json` | OK |
| 14 | `i18n` preserves HTML (`innerHTML`) | OK |
| 15 | `rka.sh` sources `urls.sh` | OK |
| 16 | `urls.sh` defines `RKA_TOKEN` | OK |
| 17 | `customize.sh` runs `migrate.sh` | OK |
| 18 | `cleanup.sh` polls `sys.boot_completed` | OK |
| 19 | `znctl.sh` uses `awk` (not `sort -V`) | OK |
| 20 | `boot-completed.sh` has KSU guard | OK |
| 21 | `customize.sh` uses `$ARCH` mapping (no `getprop`) | OK |
| 22 | `lang/` directory present in webroot | OK |
| 23 | `lspmcfg.sh` uses `$MODDIR` (no hardcoded paths) | OK |
| 24 | No sqlite3 binaries bundled in `rka/` | OK |
| 25 | `kernelsu` in `optionalDependencies` (not `dependencies`) | OK |

### 4. Old Directory Cleanup

- **`Module/Yuri/`** → Backed up to `Module/Yuri.bak/`, then deleted. Contains old monolithic scripts (21 files, 5 sqlite3 binaries).
- **`Module/webroot/common/assets/`** → Deleted (10 unreferenced asset files, 228K)
- **`Module/webroot/common/boot_hash.sh`** → Deleted (duplicate of `features/boot_hash.sh`)
- **`Module/webroot/js/`** → Deleted (stale Beer CSS JS files, 13 files)
- **`Module/webroot/css/`** → Deleted (stale Beer CSS style.css)
- **`Module/webroot/yurikey.png`** → Deleted (unreferenced)

### 5. Git Tracking

- `key` — already removed from tracking (Stream 8) ✅
- `attestation` — never tracked ✅
- `config.json` — **NOT removed** (stays tracked as download source for HMA config) ✅

### 6. Final State

```
Module/
├── module.prop
├── META-INF/com/google/android/updater-script
├── lib/       (5 files: paths.sh, urls.sh, common.sh, config_env.sh, package_list.sh)
├── features/  (14 files: keybox, target, security_patch, boot_hash, pif, hma, znctl, rka, cleanup, gms, kill_all, widevine, lsposed, migrate)
├── orchestrator.sh
├── pipelines/ (2 files: full_integrity, root_hide)
├── customize.sh
├── service.sh
├── boot-completed.sh
├── uninstall.sh
├── action.sh
├── rka/       (2 files: jsonarray.sh, lspmcfg.sh — NO bundled sqlite3)
└── webroot/
    ├── index.html (Parcel-bundled)
    ├── config.json
    ├── webroot.*.js (Parcel bundles)
    ├── webroot.*.css (Parcel bundles)
    ├── common/device-info.sh
    ├── json/dev.json
    └── lang/ (27 translation files)
```

## Files Modified

- `src/webroot/js/bridge.js` — added `exec()` export
- `src/webroot/common/device-info.sh` — replaced hardcoded paths with `$MODDIR` sourcing

## Files Created

- `src/module.prop` — copied from `Module/module.prop`
- `src/META-INF/` — copied from `Module/META-INF/`
- `package-lock.json` — generated by `npm install`

## Files Deleted

- `Module/Yuri/` — entire directory (backed up to `Module/Yuri.bak/`)
- `Module/webroot/common/assets/` — whole directory
- `Module/webroot/common/boot_hash.sh`
- `Module/webroot/js/` — entire directory
- `Module/webroot/css/` — entire directory
- `Module/webroot/yurikey.png`

---

## Checklist

- [x] `npm run build` completes without errors
- [x] Module structure is complete (including config_env.sh, migrate.sh)
- [x] No hardcoded `/data/adb/modules/Yurikey` in shell scripts
- [x] Feature scripts use `../lib/` not `lib/`
- [x] Root-level scripts use `$MODDIR/lib/` not `../lib/`
- [x] `customize.sh` uses `$MODPATH` not `$MODDIR`
- [x] `action.sh` uses context detection (`exit`/`return` based on `$0`)
- [x] No `return` in feature scripts (they use `exit`)
- [x] No `su -c` in feature scripts
- [x] `config_env.sh` exists with `cfg_get`/`cfg_set`/`cfg_delete`
- [x] No bundled sqlite3 binaries in `rka/`
- [x] `device-info.sh` sources from `lib/`
- [x] `key`, `attestation` removed from git tracking (NOT config.json)
- [x] Old directories cleaned up with rollback safety (backup before delete)
- [x] `features/rka.sh` sources `urls.sh` and uses `$RKA_TOKEN`
- [x] `lib/urls.sh` defines `RKA_TOKEN`
- [x] `customize.sh` runs `migrate.sh` during install
- [x] `cleanup.sh` polls `sys.boot_completed` before `settings put`
- [x] `znctl.sh` uses `awk` not `sort -V`
- [x] `boot-completed.sh` has KSU guard at top
- [x] `customize.sh` uses `$ARCH` mapping not `getprop`
- [x] `Module/webroot/lang/` exists with translation files
- [x] `kernelsu` is in `optionalDependencies`
- [x] All changes ready for commit
